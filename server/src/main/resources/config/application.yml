management:
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true
    diskspace:
      enabled: true

logging:
  level:
    uk:
      co:
        telegraph:
          switchboard: DEBUG

spring:
  output:
    ansi:
      enabled: ALWAYS
